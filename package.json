{
  "name": "clueless-interactions",
  "version": "1.0.8",
  "description": "This is an interactions tracker to track things like button presses and other interactions with your app.",
  "main": "dist/interaction-tracker.js",
  "prisma": {
    "schema": "prisma/schema.prisma"
  },
  "scripts": {
    "prisma:generate": "prisma generate",
    "build": "prisma generate && tsc",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "interactions",
    "tracker",
    "analytics",
    "typescript"
  ],
  "author": "bcuff",
  "license": "MIT",
  "type": "module",
  "devDependencies": {
    "@types/node": "^24.0.12",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "@prisma/client": "^6.11.1",
    "prisma": "^6.11.1"
  },
  "types": "dist/types/interaction-tracker.d.ts"
}
